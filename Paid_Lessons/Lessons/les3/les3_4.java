package Paid_Lessons.Modules.Module_1.les3;

public class les3_4 {
    public static void main(String[] args) {
        short x = 3, a = 12;
        int res = x < a ? (x + a) : (x - a); //Если x < a, то выполняется x + a, иначе x - a
        System.out.println(res);
    }
}
